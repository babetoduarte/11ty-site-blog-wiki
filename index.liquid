---
layout: layouts/base.njk
---

<h1>{{ metadata.title }}</h1>

<h2>Latest 5 Blog Posts</h2>
<ul>
  {% for post in collections.post limit:5 reversed %}
  <li><a href="{{post.url}}">{{ post.data.title }}</a> - {{ post.date | postDate}}</li>
  {% endfor %}
</ul>

<h2>Latest 2 Wiki Articles</h2>
<ul>
  {% for article in collections.article limit:2 reversed %}
  <li><a href="{{article.url}}">{{ article.data.title }}</a> - {{ article.date | postDate}}</li>
  {% endfor %}
</ul>
